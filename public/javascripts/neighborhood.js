var NEIGHBORHOOD = (function(){
  
  //private, closed up into NEIGHBORHOOD namespace

  
  
  return {
    //caller can only access what is defined here
    switchCategory : function(){},
    //if doesn't alredy exist , just add as option
  		//if does already exist, draw
    addCategory : function(){},
    draw : function() //pass map & optional list of categories
  };
})();  //map